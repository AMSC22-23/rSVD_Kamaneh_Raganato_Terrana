name: Parametric Diffusion 1D

on:
  push:
    paths: 
      - 'POD/ParametricDiffusion1D/**'
      - '!POD/ParametricDiffusion/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Configure CMake
      run: cmake -B ${{github.workspace}}/POD/ParametricDiffusion1D/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Setup MPI
      uses: mpi4py/setup-mpi@v1

    - name: Build
      run: cmake --build ${{github.workspace}}/POD/ParametricDiffusion1D/build

    - name: Run test on parametric diffusion
      run: mpirun -n 4 ./build/ParametricDiffusion1D
